# The vendor 6.1 mdio_bus emits an OF-style MODALIAS for PHYs with a device
# node, so the driver's mdio-id alias never matches and udev cannot autoload
# it. Load it explicitly: the generic C22 PHY still runs the link, but only
# this driver programs the RJ45 LED mode.
maxio
