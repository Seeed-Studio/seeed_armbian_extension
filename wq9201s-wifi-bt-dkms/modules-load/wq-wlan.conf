# wq_wlan has no udev autoload: the pre-firmware SDIO enumeration carries no
# modalias that maps to the driver. btattach needs hci_uart's N_HCI line
# discipline. Load both explicitly at boot.
hci_uart
wq_wlan
