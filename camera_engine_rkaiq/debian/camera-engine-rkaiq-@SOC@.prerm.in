#!/bin/sh
set -e

if [ "$1" = "remove" ] || [ "$1" = "deconfigure" ]; then
    if command -v systemctl >/dev/null 2>&1; then
        systemctl stop rkaiq_3A.service || true
        systemctl disable rkaiq_3A.service || true
    fi
fi

exit 0
